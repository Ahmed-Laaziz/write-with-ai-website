// import jsPDF from 'jspdf';

// const generatePDF = (textSections) => {
//   const doc = new jsPDF();
//   doc.setFontSize(12);

//   textSections.forEach((section, index) => {
//     doc.text(section, 10, 10 + index * 10);
//   });

//   return doc.output('blob');
// };

// export default generatePDF;
